package com.manruhomerun.yadanbeopseok.designsystem.theme

import androidx.compose.ui.graphics.Color

/**
 * 야단법석의 대표 색상입니다.
 *
 * HTML 디자인의 기본 스카이 블루와 여기서 파생되는
 * 강조·배경·텍스트 색상을 사용합니다.
 */
val YadanPrimary = Color(0xFF52A1FF)
val YadanPrimaryDark = Color(0xFF3E7AC2)
val YadanPrimaryTint = Color(0xFFE6EFFB)
val YadanPrimaryTintStrong = Color(0xFFD8E8FB)
val YadanPrimaryInk = Color(0xFF3E6EA8)
val YadanOnPrimary = Color(0xFFFFFFFF)

/**
 * 화면과 컴포넌트의 배경 색상입니다.
 *
 * 화면 전체에는 오프 화이트를 사용하고,
 * 카드와 입력 영역에는 흰색을 사용합니다.
 */
val YadanBackground = Color(0xFFFAFAFA)
val YadanSurface = Color(0xFFFFFFFF)

/**
 * 텍스트에 사용하는 색상입니다.
 */
val YadanTextPrimary = Color(0xFF1C2024)
val YadanTextSecondary = Color(0xFF3F454B)
val YadanTextMuted = Color(0xFF8A9099)

/**
 * 테두리와 구분선에 사용하는 색상입니다.
 */
val YadanOutline = Color(0xFFE2E4E7)
val YadanDivider = Color(0xFFEEF0F2)

/**
 * 장소의 접근성 및 동행 배려 정보를 표시할 때 사용하는 색상입니다.
 */
val YadanAccessibilityBackground = Color(0xFFE8EEF6)
val YadanAccessibilityContent = Color(0xFF33567E)
val YadanCareBackground = Color(0xFFE7F3EC)
val YadanCareContent = Color(0xFF1F7A48)

/**
 * 카카오 로그인 버튼에 사용하는 공식 서비스 색상입니다.
 */
val KakaoBackground = Color(0xFFFEE500)
val KakaoContent = Color(0xFF181600)

/**
 * 오류 메시지와 경고 상태에 사용하는 색상입니다.
 */
val YadanError = Color(0xFFE0392B)
val YadanOnError = Color(0xFFFFFFFF)
